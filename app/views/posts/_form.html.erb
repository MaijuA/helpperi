<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= @post.errors.count %>
        <% if @post.errors.count == 1 %>
            virhe:
        <% else %>
            virhettä:
        <% end %>
      </h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :'Otsikko' %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :'Kuvaus' %><br>
    <%= f.text_field :description %>
  </div>
  <%if not @edit %>
    <div class="actions">
      <%= f.submit 'Luo ilmoitus' %>
    </div>
  <% else %>
    <div class="actions">
      <%= f.submit 'Muokkaa ilmoitusta' %>
    </div>
  <% end %>
<% end %>
