<h1>Ilmoitukset</h1>

<table>
  <thead>
    <tr>
      <th class="col-md-3">Otsikko</th>
      <th class="col-md-3">Kuvaus</th>
      <th class="col-md-3">Hinta</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td class="col-md-3"><%= truncate(post.title, :length => 30) %></td>
        <td class="col-md-3"><%= truncate(post.description, :length => 30) %></td>
        <td class="col-md-3"><%= post.price %></td>
        <td><%= link_to 'Näytä ilmoitus', post %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<%= link_to 'Uusi ilmoitus', new_post_path if current_user %>
