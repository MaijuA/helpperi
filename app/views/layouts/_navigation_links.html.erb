<li><%= link_to 'Etusivu', root_path %></li>
<% if user_signed_in? %>
    <li><%= link_to users_path do %>
          Omat ilmoitukset <%= content_tag :span, post_candidate_changes_count,
                                           class: 'label label-warning' if post_candidate_changes_count > 0 %>
          <%= content_tag :span, posts_conversations_count,
                          class: 'label label-primary' if posts_conversations_count > 0 %>
      <% end %>
    </li>
    <li><%= link_to user_interests_path do %>
          Omat kiinnostukset <%= content_tag :span, users_candidate_changes_count,
                                             class: 'label label-warning' if users_candidate_changes_count > 0 %>
          <%= content_tag :span, candidate_conversations_count,
                          class: 'label label-primary' if candidate_conversations_count > 0 %>
      <% end %>
    </li>

    <li>
      <%= link_to conversations_path, class: ""  do %>
          <span class="glyphicon glyphicon-envelope white"></span>
          <%= content_tag :span, users_conversations_count,
                          class: 'label label-primary' if users_conversations_count > 0 %>
      <% end %>
    </li>

    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <span class="glyphicon glyphicon-user white"></span> <%= current_user.first_name %> <span class="caret"></span></a>
      <ul class="dropdown-menu">
        <li><%= link_to 'Omat tiedot', user_path(current_user) %></li>
        <li role="separator" class="divider"></li>
        <li><%= link_to 'Kirjaudu ulos', destroy_user_session_path, :method => :delete %></li>
      </ul>
    </li>

<% else %>
    <li><%= link_to 'Kirjaudu', user_session_path %></li>
    <li><%= link_to 'Rekisteröidy', new_user_registration_path %></li>
<% end %>
